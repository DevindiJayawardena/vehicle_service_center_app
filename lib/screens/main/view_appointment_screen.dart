import 'package:flutter/material.dart';

import '../../widgets/template/main/view_appointment_template.dart';

class ViewAppointmentScreen extends StatelessWidget {
  const ViewAppointmentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ViewAppointmentTemplate();
  }
}
